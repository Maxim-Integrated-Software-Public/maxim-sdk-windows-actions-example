name: Test

on: [push]

jobs:
  build:
    name: Build Firmware
    runs-on: windows-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Nuget
        run: |
          nuget
          
      - name: MSBuild
        run: |
          msbuild
          
